on: 
  push:
    branches:
      - master





jobs:
  build:
    runs-on: ubuntu-latest


    steps: 
      - name: 'testing printing before'
        run: ls
      - uses: actions/checkout@v3
      - name: 'testing printing after'
        run: ls
